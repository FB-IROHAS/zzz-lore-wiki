{
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "scripts": {
    "docs:dev": "node scripts/generate-terminology-pages.mjs && vitepress dev docs",
    "docs:build": "node scripts/generate-terminology-pages.mjs && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  }
}
